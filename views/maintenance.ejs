<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title><%= storeName %> • Maintenance Mode</title>
  
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --bg-deep: #03050a;
      --bg-dark: #0c0f15;
      --bg-card: rgba(12, 15, 21, 0.8);
      --accent-cyan: #00f0ff;
      --accent-blue: #3b82f6;
      --accent-purple: #8b5cf6;
      --accent-glow: rgba(0, 240, 255, 0.5);
      --warning-neon: #ffb86b;
      --warning-glow: rgba(255, 184, 107, 0.4);
      --text-primary: #f1f5f9;
      --text-secondary: #94a3b8;
      --border-glow: rgba(0, 240, 255, 0.15);
      --font-display: 'Space Grotesk', sans-serif;
      --font-mono: 'JetBrains Mono', monospace;
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      background: var(--bg-deep);
      color: var(--text-primary);
      font-family: var(--font-display);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow-x: hidden;
    }
    
    /* Cyberpunk Grid Background */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: 
        linear-gradient(rgba(0, 240, 255, 0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0, 240, 255, 0.03) 1px, transparent 1px);
      background-size: 40px 40px;
      pointer-events: none;
      z-index: 0;
    }
    
    /* Glowing Orbs */
    body::after {
      content: '';
      position: fixed;
      top: -20%;
      right: -10%;
      width: 600px;
      height: 600px;
      background: radial-gradient(circle, rgba(59,130,246,0.1) 0%, transparent 70%);
      filter: blur(60px);
      z-index: 0;
      animation: orbFloat 20s ease-in-out infinite;
    }
    
    .orb-left {
      position: fixed;
      bottom: -20%;
      left: -10%;
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, rgba(139,92,246,0.08) 0%, transparent 70%);
      filter: blur(60px);
      z-index: 0;
      animation: orbFloat 25s ease-in-out infinite reverse;
    }
    
    @keyframes orbFloat {
      0%, 100% { transform: translate(0, 0) scale(1); }
      50% { transform: translate(-5%, 5%) scale(1.1); }
    }
    
    .container {
      max-width: 700px;
      padding: 40px 24px;
      text-align: center;
      position: relative;
      z-index: 10;
    }
    
    /* Holographic Card */
    .maintenance-card {
      background: var(--bg-card);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid var(--border-glow);
      border-radius: 32px;
      padding: 48px 32px;
      box-shadow: 
        0 20px 40px -20px rgba(0,0,0,0.8),
        0 0 0 1px rgba(0, 240, 255, 0.05),
        0 0 20px rgba(0, 240, 255, 0.1);
      position: relative;
      overflow: hidden;
    }
    
    /* Animated Border */
    .maintenance-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 200%;
      height: 2px;
      background: linear-gradient(
        90deg,
        transparent,
        var(--accent-cyan),
        var(--accent-purple),
        var(--accent-blue),
        transparent
      );
      animation: borderScan 8s linear infinite;
    }
    
    @keyframes borderScan {
      0% { left: -100%; }
      100% { left: 100%; }
    }
    
    /* Floating Icon Container */
    .icon-container {
      width: 140px;
      height: 140px;
      background: linear-gradient(
        135deg,
        rgba(255, 184, 107, 0.15),
        rgba(255, 184, 107, 0.05)
      );
      border: 2px solid rgba(255, 184, 107, 0.3);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 32px;
      font-size: 4.5rem;
      color: var(--warning-neon);
      box-shadow: 
        0 0 30px rgba(255, 184, 107, 0.2),
        0 0 60px rgba(255, 184, 107, 0.1);
      position: relative;
      animation: hologramFloat 4s ease-in-out infinite;
    }
    
    /* Pulsing Ring */
    .icon-container::after {
      content: '';
      position: absolute;
      inset: -10px;
      border: 2px solid rgba(255, 184, 107, 0.2);
      border-radius: 50%;
      animation: ringPulse 3s ease-out infinite;
    }
    
    @keyframes ringPulse {
      0% {
        inset: -10px;
        border-width: 2px;
        opacity: 0.8;
      }
      100% {
        inset: -30px;
        border-width: 1px;
        opacity: 0;
      }
    }
    
    @keyframes hologramFloat {
      0%, 100% { transform: translateY(0) scale(1); }
      50% { transform: translateY(-8px) scale(1.02); }
    }
    
    /* Glitch Text Effect */
    .glitch-wrapper {
      position: relative;
      margin-bottom: 16px;
    }
    
    h1 {
      font-size: 3.2rem;
      font-weight: 800;
      letter-spacing: -2px;
      text-transform: uppercase;
      background: linear-gradient(135deg, #fff, #cbd5e1, #fff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      position: relative;
      display: inline-block;
      animation: glitch 3s infinite;
    }
    
    @keyframes glitch {
      0%, 100% { transform: translate(0); }
      92% { transform: translate(0); }
      93% { transform: translate(-2px, 1px); }
      94% { transform: translate(2px, -1px); }
      95% { transform: translate(-1px, 0); }
      96% { transform: translate(1px, -1px); }
      97% { transform: translate(0, 1px); }
    }
    
    .cyber-subtitle {
      font-size: 1.1rem;
      color: var(--text-secondary);
      line-height: 1.7;
      margin-bottom: 40px;
      position: relative;
      display: inline-block;
      padding: 0 20px;
    }
    
    .cyber-subtitle::before,
    .cyber-subtitle::after {
      content: '〈';
      position: absolute;
      left: -5px;
      color: var(--accent-cyan);
      opacity: 0.5;
      font-family: var(--font-mono);
    }
    
    .cyber-subtitle::after {
      content: '〉';
      left: auto;
      right: -5px;
    }
    
    /* Cyber Message Box */
    .message-box {
      background: rgba(0, 240, 255, 0.03);
      border: 1px solid rgba(0, 240, 255, 0.15);
      border-left: 6px solid var(--warning-neon);
      border-radius: 16px;
      padding: 28px;
      margin-bottom: 40px;
      text-align: left;
      backdrop-filter: blur(10px);
      position: relative;
      transition: all 0.3s ease;
    }
    
    .message-box:hover {
      background: rgba(0, 240, 255, 0.05);
      border-color: rgba(255, 184, 107, 0.3);
      transform: translateX(5px);
    }
    
    .message-label {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 6px 16px;
      background: rgba(255, 184, 107, 0.12);
      border: 1px solid rgba(255, 184, 107, 0.3);
      color: var(--warning-neon);
      border-radius: 100px;
      font-size: 0.8rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-bottom: 16px;
      font-family: var(--font-mono);
    }
    
    .message-label i {
      font-size: 0.9rem;
    }
    
    .message-text {
      font-size: 1.1rem;
      color: var(--text-primary);
      font-family: var(--font-mono);
      line-height: 1.6;
      padding-left: 8px;
      border-left: 2px solid var(--accent-cyan);
      position: relative;
    }
    
    /* Status Indicators */
    .status-bar {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 24px;
      margin-bottom: 32px;
      flex-wrap: wrap;
    }
    
    .status-item {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 0.85rem;
      color: var(--text-secondary);
      font-family: var(--font-mono);
      padding: 6px 16px;
      background: rgba(0,0,0,0.3);
      border-radius: 100px;
      border: 1px solid rgba(255,255,255,0.05);
    }
    
    .status-dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: var(--warning-neon);
      box-shadow: 0 0 15px var(--warning-glow);
      animation: blink 2s infinite;
    }
    
    @keyframes blink {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }
    
    /* Admin Section - Small & Subtle */
    .admin-section {
      margin-top: 40px;
      padding-top: 30px;
      border-top: 1px solid rgba(0, 240, 255, 0.1);
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 16px;
    }
    
    .admin-tooltip {
      font-size: 0.7rem;
      color: var(--text-secondary);
      letter-spacing: 2px;
      text-transform: uppercase;
      opacity: 0.5;
      display: flex;
      align-items: center;
      gap: 8px;
      font-family: var(--font-mono);
    }
    
    .admin-tooltip i {
      color: var(--accent-cyan);
      font-size: 0.65rem;
    }
    
    .admin-links {
      display: flex;
      gap: 12px;
      align-items: center;
      justify-content: center;
      flex-wrap: wrap;
    }
    
    /* Admin Login - Very Small */
    .admin-link-small {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 14px;
      background: rgba(99, 102, 241, 0.08);
      border: 1px solid rgba(99, 102, 241, 0.15);
      color: #a5b4fc;
      text-decoration: none;
      border-radius: 6px;
      font-size: 0.7rem;
      font-weight: 500;
      transition: all 0.2s ease;
      font-family: var(--font-mono);
      letter-spacing: 0.5px;
      text-transform: uppercase;
    }
    
    .admin-link-small:hover {
      background: rgba(99, 102, 241, 0.15);
      border-color: rgba(99, 102, 241, 0.3);
      color: white;
      transform: translateY(-1px);
    }
    
    .admin-link-small i {
      font-size: 0.7rem;
    }
    
    /* Contact Button - Minimal */
    .contact-mini {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 14px;
      background: rgba(255,255,255,0.02);
      border: 1px solid rgba(255,255,255,0.05);
      color: var(--text-secondary);
      text-decoration: none;
      border-radius: 6px;
      font-size: 0.7rem;
      transition: all 0.2s ease;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      font-family: var(--font-mono);
    }
    
    .contact-mini:hover {
      background: rgba(37, 211, 102, 0.08);
      border-color: rgba(37, 211, 102, 0.2);
      color: #25d366;
    }
    
    .contact-mini i {
      font-size: 0.7rem;
    }
    
    /* Store Name - Cyber Style */
    .store-name {
      position: fixed;
      bottom: 24px;
      left: 0;
      right: 0;
      color: rgba(148, 163, 184, 0.5);
      font-size: 0.75rem;
      font-family: var(--font-mono);
      letter-spacing: 3px;
      text-transform: uppercase;
      z-index: 20;
    }
    
    .store-name span {
      color: var(--accent-cyan);
      opacity: 0.7;
    }
    
    /* Particle Effects */
    .particle {
      position: fixed;
      width: 2px;
      height: 2px;
      background: var(--accent-cyan);
      border-radius: 50%;
      opacity: 0.3;
      animation: particleFloat 10s linear infinite;
    }
    
    @keyframes particleFloat {
      0% {
        transform: translateY(0) translateX(0);
        opacity: 0;
      }
      10% {
        opacity: 0.5;
      }
      90% {
        opacity: 0.5;
      }
      100% {
        transform: translateY(-100vh) translateX(100px);
        opacity: 0;
      }
    }
    
    /* Responsive */
    @media (max-width: 768px) {
      .container { padding: 20px 16px; }
      .maintenance-card { padding: 32px 20px; }
      h1 { font-size: 2.2rem; }
      .icon-container { width: 100px; height: 100px; font-size: 3rem; }
      .status-bar { gap: 12px; }
      .status-item { font-size: 0.75rem; padding: 4px 12px; }
    }
    
    @media (max-width: 480px) {
      h1 { font-size: 1.8rem; }
      .message-text { font-size: 0.95rem; }
      .cyber-subtitle { font-size: 0.95rem; }
      .admin-links { flex-direction: column; gap: 8px; }
      .admin-link-small, .contact-mini { width: 100%; justify-content: center; }
    }
  </style>
</head>
<body>
  <!-- Animated Orbs -->
  <div class="orb-left"></div>
  
  <!-- Particles - Cyberpunk Rain -->
  <div class="particle" style="top: 20%; left: 10%; animation-delay: 0s;"></div>
  <div class="particle" style="top: 40%; left: 30%; animation-delay: 2s;"></div>
  <div class="particle" style="top: 60%; left: 70%; animation-delay: 4s;"></div>
  <div class="particle" style="top: 80%; left: 90%; animation-delay: 6s;"></div>
  <div class="particle" style="top: 10%; left: 50%; animation-delay: 8s;"></div>
  <div class="particle" style="top: 70%; left: 20%; animation-delay: 3s;"></div>
  <div class="particle" style="top: 30%; left: 80%; animation-delay: 7s;"></div>
  <div class="particle" style="top: 90%; left: 40%; animation-delay: 5s;"></div>
  <div class="particle" style="top: 15%; left: 15%; animation-delay: 1s;"></div>
  <div class="particle" style="top: 45%; left: 85%; animation-delay: 9s;"></div>
  <div class="particle" style="top: 75%; left: 55%; animation-delay: 11s;"></div>
  <div class="particle" style="top: 25%; left: 95%; animation-delay: 13s;"></div>

  <div class="container">
    <div class="maintenance-card">
      
      <!-- Icon Container with Cyber Effect -->
      <div class="icon-container">
        <i class="fas fa-microchip"></i>
      </div>
      
      <!-- Glitch Title -->
      <div class="glitch-wrapper">
        <h1>SYSTEM MAINTENANCE</h1>
      </div>
      
      <div class="cyber-subtitle">
        ⚡ UPGRADE IN PROGRESS ⚡
      </div>
      
      <!-- Status Bar - Live System Status -->
      <div class="status-bar">
        <div class="status-item">
          <span class="status-dot"></span>
          <span>SYS: OFFLINE</span>
        </div>
        <div class="status-item">
          <i class="fas fa-sync-alt" style="color: var(--accent-cyan);"></i>
          <span>UPGRADE: 73%</span>
        </div>
        <div class="status-item">
          <i class="fas fa-clock" style="color: var(--accent-cyan);"></i>
          <span>ETA: 15 MIN</span>
        </div>
      </div>
      
      <!-- Announcement Box - Cyber Style -->
      <div class="message-box">
        <div class="message-label">
          <i class="fas fa-bullhorn"></i> 
          ADMIN ANNOUNCEMENT
          <span style="margin-left: 8px; color: var(--warning-neon);">● LIVE</span>
        </div>
        <div class="message-text">
          <%= message %>
        </div>
      </div>
      
      <!-- Minimal Admin & Contact Section - Small & Subtle -->
      <div class="admin-section">
        <div class="admin-tooltip">
          <i class="fas fa-lock"></i>
          <span>RESTRICTED ACCESS</span>
          <i class="fas fa-lock"></i>
        </div>
        
        <div class="admin-links">
          <!-- Admin Login - Very Small, No Button Style -->
          <a href="/admin" class="admin-link-small">
            <i class="fas fa-shield-hal"></i>
            ADMIN
          </a>
          
          <!-- Contact Admin - Minimal, Sidebar Style -->
          <a href="<%= config?.CONTACT_ADMIN || 'https://wa.me/6281249703469' %>" class="contact-mini" target="_blank">
            <i class="fab fa-whatsapp"></i>
            CONTACT
          </a>
        </div>
        
        <div style="font-size: 0.65rem; color: rgba(148, 163, 184, 0.3); margin-top: 8px; font-family: var(--font-mono);">
          ________________________________
        </div>
      </div>
      
    </div>
  </div>
  
  <!-- Store Name - Cyber Footer -->
  <div class="store-name">
    <span>〈</span> <%= storeName %> © <%= new Date().getFullYear() %> <span>〉</span>
  </div>
  
  <script>
    // Dynamic status update simulation (optional)
    document.addEventListener('DOMContentLoaded', function() {
      const upgradeEl = document.querySelector('.status-item:nth-child(2) span:last-child');
      const etaEl = document.querySelector('.status-item:nth-child(3) span:last-child');
      
      if (upgradeEl && etaEl) {
        let percentage = 73;
        let minutes = 15;
        
        setInterval(() => {
          percentage = Math.min(99, percentage + 1);
          minutes = Math.max(1, minutes - 1);
          
          if (percentage < 99) {
            upgradeEl.textContent = `UPGRADE: ${percentage}%`;
            etaEl.textContent = `ETA: ${minutes} MIN`;
          }
        }, 3000);
      }
    });
  </script>
</body>
</html>